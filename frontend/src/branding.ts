// One place to change the product name. "Corewise" is a working name for now, so
// if we settle on something else, this is the only spot to edit.
export const PRODUCT_PREFIX = 'Corewise';
export const PRODUCT_NAME = 'Modernization';
