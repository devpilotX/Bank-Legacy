import { PlaceholderPage } from '../components/PlaceholderPage';

export function VerifyPage() {
  return (
    <PlaceholderPage
      title="Verify"
      description="Check that the new Java behaves like the old code before anything ships."
    />
  );
}
