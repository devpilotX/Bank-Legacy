import { PlaceholderPage } from '../components/PlaceholderPage';

export function CodePage() {
  return (
    <PlaceholderPage
      title="Code"
      description="The old source files we take in for a project, ready to read and explain."
    />
  );
}
