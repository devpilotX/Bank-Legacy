import { PlaceholderPage } from '../components/PlaceholderPage';

export function ModernizePage() {
  return (
    <PlaceholderPage
      title="Modernize"
      description="Rewrite old code into Java one small piece at a time, with the AI drafting and a person approving."
    />
  );
}
