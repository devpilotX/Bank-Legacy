import { PlaceholderPage } from '../components/PlaceholderPage';

export function ProjectsPage() {
  return (
    <PlaceholderPage
      title="Projects"
      description="Each bank's body of work, moving from intake through to done."
    />
  );
}
