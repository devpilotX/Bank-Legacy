/**
 * The core things our system is about: users, clients, the projects we run for
 * those banks, and the source files we take in. The JPA entities that map to the
 * database tables go here as we build out each feature.
 */
package com.corewise.modernization.domain.model;
