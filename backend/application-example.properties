# Local development settings.
#
# Copy this file to application-local.properties in this same backend folder, then
# fill in your real values. Git ignores application-local.properties, so your
# password never gets committed.
#
#   cp application-example.properties application-local.properties
#
# The app reads application.properties first and then layers these values on top.

# Your local database password. This is usually the only thing you need to set.
spring.datasource.password=put-your-local-password-here

# Uncomment and change these only if your local setup differs from the defaults
# already in application.properties.
# spring.datasource.url=jdbc:postgresql://localhost:5432/modernization
# spring.datasource.username=modernization_app


# ---- Sign-in token secret ----
# A long random string, at least 32 characters. Generate your own, do not reuse this.
app.security.jwt.secret=replace-with-a-long-random-secret-at-least-32-chars

# ---- First admin account (used once, only when there are no users yet) ----
app.bootstrap.admin-email=admin@example.com
app.bootstrap.admin-password=change-this-admin-password
app.bootstrap.admin-name=Platform Admin


# ---- AI provider (optional for local work) ----
# Pick claude or openai, then set that provider's key. Leave the key blank to skip
# the AI locally; the app still runs and AI calls return a clear "not set up" error.
#
# Use a strong, current model. Verification needs the translated Java to actually
# compile and run, so a weak model is a poor fit. We run a strong Claude (Sonnet 4.5)
# through OpenRouter, which is OpenAI compatible, set up like this:
#   app.ai.provider=openai
#   app.ai.openai.base-url=https://openrouter.ai/api
#   app.ai.openai.model=anthropic/claude-sonnet-4.5
#   app.ai.openai.api-key=your-openrouter-key
app.ai.provider=claude
app.ai.claude.api-key=your-anthropic-api-key
# app.ai.openai.api-key=your-openai-api-key
